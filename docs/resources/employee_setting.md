---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kala_employee_setting Resource - kala"
subcategory: ""
description: |-
  Manages a single setting on an existing Kala employee.
  **Kala cannot delete settings.** `terraform destroy` removes this resource from state and warns; the setting remains on the employee. See ADR-001.
---

# kala_employee_setting (Resource)

Manages a single setting on an existing Kala employee.

> **Kala cannot delete settings.** `terraform destroy` removes this resource from state and warns; the setting remains on the employee. See ADR-001.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `employee_number` (Number) Kala employee number. Changing this forces replacement.
- `friendly_name` (String) Human-readable label. **Write-only:** Kala requires it on every write but never returns it from any read endpoint, so Terraform cannot detect drift on it. Changing it re-sends the setting but cannot be verified.
- `key` (String) Setting key. Changing this forces replacement — and note that the old key is **not** removed, because Kala offers no way to delete a setting.
- `value` (String) Setting value. This is the only field Kala returns on read, so it is the only one drift can be detected on.

### Optional

- `allow_new_key` (Boolean) Permit a `key` not already in use anywhere on the account. Defaults to `false` because Kala cannot delete settings: a mistyped key stays on the employee's record permanently. Set to `true` when deliberately introducing a new setting.
- `type` (String) Setting type. **Write-only**, like `friendly_name`. Defaults to `text`.

### Read-Only

- `id` (String) Synthetic identifier, `<employee_number>:<key>`.
